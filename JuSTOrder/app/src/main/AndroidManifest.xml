<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jstorder_app">
    <queries>
        <package android:name="com.google.android.apps.maps" />
    </queries>

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JSTOrder_APP">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <activity android:name=".MapsActivity1"/>

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login_with_otp" />
        <activity android:name=".Check_email" />
        <activity android:name=".Sign_up" />
        <activity android:name=".Login_with_phone_otp" />
        <activity android:name=".Delivery_ui" />
        <activity android:name=".Combo_Activity" />
        <activity android:name=".NandanaPalace_Activity" />
        <activity android:name=".Hyderabad_biryani_Activity" />
        <activity android:name=".Biryani_Activity" />
        <activity android:name=".Cart_Activity" />
        <activity android:name=".Refer_earn_Activity" />
        <activity android:name=".Spin_Activity" />
        <activity android:name=".Contact_Us_Activity" />
        <activity android:name=".Wallet_Activity" />
        <activity android:name=".Treat_Activity" />
        <activity android:name=".Order_History_Activity" />

        <activity android:name=".Payment_Gateway" />
    </application>

</manifest>